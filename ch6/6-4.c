/* Modified program to generate prime numbers */
#include <stdio.h>
#include <stdbool.h>


int main(void) {

    int i;
    int p;
    int primes[50];
    int primeIndex = 2;

    bool isPrime;


    primes[0] = 2;
    primes[1] = 3;

    for (p = 5; p <= 50; p = p + 2) {
        isPrime = true;

        for (i = 1; isPrime && p / primes[i][ >= ])
    }

    return 0;

}